@model FODT.Views.Shows.SortedShowsViewModel

<center>
    <h1>All shows sorted by year</h1>  
    @foreach (var item in Model.Shows)
    {
        <a href="@item.ShowLinkURL">@ExtensionMethods.RearrangeShowTitle(item.ShowTitle) (@item.ShowQuarter @item.ShowYear)</a><br>
    }
</center>