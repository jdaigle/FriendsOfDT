@model FODT.Views.Person.ListPersonPhotosViewModel

<div style="text-align:center;">
    <h3>Photos for <a href="@Model.PersonLinkURL">@Model.PersonFullname</a></h3>
    @if (Model.Photos.Count == 0)
    {
        <h4>No associated photos found!</h4><h4>Would you like to <a href="@Model.PhotoUploadLinkURL">upload some</a>?</h4>
    }
</div>
@if (Model.Photos.Any())
{
    <div style="text-align:center;">
        <p>
            <a href="@Model.PhotoUploadLinkURL">Upload more images for this person</a>
        </p>
    </div>
    var rowCount = Math.Ceiling((double)Model.Photos.Count / 4d);
    <table class="table media">
        <tbody>
            @for (int i = 0; i < rowCount; i++)
            {
                <tr>
                    @foreach (var item in Model.Photos.Skip(i * 4).Take(4))
                    {
                        <td>
                            <a href="@item.PhotoLinkURL">
                                <img src="@item.PhotoThumbnailURL"></a>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
    <div style="text-align:center;">
        <p>
            <a href="@Model.PhotoUploadLinkURL">Upload more images for this person</a>
        </p>
    </div>
}