@* Generator: WebPagesHelper *@

@using FODT.Models
@using FODT.Views.Show

@helper RenderColumns(CrewPositionViewModel item)
{
    <td>@item.ShowQuarter.ToString() @item.ShowYear</td>
    <td><a href="@item.ShowLinkURL">@item.ShowName</a></td>
    <td>@item.Name</td>
}